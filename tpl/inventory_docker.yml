connection_type: ssh
filters:
- include: inventory_hostname.startswith('ap')
- exclude: true
# docker_name is populated by the inv
# docker_config maps to `docker inspect *id*` "Config" field
groups: {}
plugin: community.docker.docker_containers
strict: true
