groups:
  search:
    count: 5
    container: base
  clustermanager:
    count: 3
    container: base
  indexer:
    count: 5
    container: base
  deployer:
    count: 1
    container: base
  licensemanager:
    count: 1
    container: base


containers:
  base:
    image: valshatravenko/debian-sshd
    ports:
      - 8089
    volumes:
      - $HOME/.ssh/id_ed25519.pub:/etc/sshd/authorized_keys # figure out the optimal destination
      - $docker_vol_data:/opt/splunk
